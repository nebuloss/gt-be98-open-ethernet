# Build artifacts — builds happen on dev-build, never committed here
*.o
*.ko
*.ko.cmd
*.mod
*.mod.c
*.cmd
.tmp_versions/
modules.order
Module.symvers
*.dtb
*.dtbo

# QEMU artifacts — staged on dev-build (~/qemu-be98), never committed here
*.cpio
*.cpio.gz
Image
boot.log
qemu/**/build/

# RE binaries / firmware dumps — staged on CT 310 (/opt/re-bins), not in git
re-bins/
*.bin
*.elf
*.img
!qemu/**/*.elf.note   # allow small annotated notes if any

# Internal-only docs + config — kept locally, NEVER published to the public repo
.mcp.json
docs/gt-be98-open-ethernet-orchestrator-prompt.md
docs/recovery-plan.md
docs/build-env.md
# live-test-runbook references device recovery internals (slots/U-Boot/watchdog)
docs/live-test-runbook.md
# extract-runner-microcode documents proprietary microcode symbol offsets
tools/extract-runner-microcode.sh

# Secrets / local
*.local
*-psk*
*.key
*.pem
secrets*

# Editor / OS
.serena/
.vscode/
*.swp
*~
.DS_Store

# Local harness config (not published)
.claude/

# raw real-HW trial logs (may contain device IPs/MACs) - keep local, commit redacted summaries only
re-notes/realhw/raw/
